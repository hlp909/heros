<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Hero - Admin</title>
  <link rel="stylesheet" href="/node_modules/bootstrap/dist/css/bootstrap.css">
</head>

<body>
  <header>
    <div class="page-header container">
      <h1><a href="/">中国女排</a> <small>英雄管理器</small></h1>
    </div>
  </header>
  <div class="container hero-list">
    <form id="form">
      <div class="form-group">
        <label>女排名称</label>
        <input type="text" name="name" class="form-control" placeholder="请输入英雄名称">
      </div>
      <div class="form-group">
        <label>女排性别</label>
        <div class="radio">
          <label>
            <input type="radio" name="gender" value="男" checked>男
          </label>
          <label>
            <input type="radio" name="gender" value="女">女
          </label>
        </div>
      </div>
      <div class="form-group">
        <label>女排年龄</label>
        <input type="text" name="age" class="form-control" placeholder="请输入女排年龄">
      </div>
      <div class="form-group">
        <label>女排手机号</label>
        <input type="text" name="phone" class="form-control" placeholder="请输入女排手机号">
      </div> 
      
      <div class="form-group">
        <label>女排住址</label>
        <input type="text" name="address" class="form-control" placeholder="请输入女排住址">
      </div> 
      <button type="submit" class="btn btn-success">点击保存</button>
    </form>
  </div>
  <script src="/node_modules/jquery/dist/jquery.js"></script>

  <script>
   $('#form').on('submit',function(e){

// 2. 阻止默认的提交行为
e.preventDefault()

// 3.发送ajax请求
$.ajax({
  type:'post',
  url:'/addHero',
  data:$(this).serialize(),
  success:function(res){
    // 4. 跳转到主页面
    if(res.code==200){
      location.href = '/'
    }
  }
})
})
  </script>
</body>

</html>



